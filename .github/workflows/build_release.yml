name: Build Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build Release
    uses: ./.github/workflows/build.yml
    with:
      cache-mode: 'read-write'
      ref: ${{ github.sha }}
